<h1> List of Users </h1>
<ul>
 <% @users.each do |user| %>
    <li> <%= link_to user.username, user_path(user) %>
    Email <%= user.email %>
    Member Status <%= user.member_status %>
    Admin Status <%= user.admin %>
<% end %>
</ul>
